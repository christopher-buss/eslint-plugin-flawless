// Coarse benchmark fixture for flawless/no-conditional-in-test.
// Each test body contains a conditional (an if statement) — the rule reports each one.
it("t0", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t1", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t2", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t3", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t4", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t5", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t6", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t7", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t8", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t9", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t10", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t11", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t12", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t13", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t14", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t15", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t16", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t17", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t18", () => { if (flag) { doThing(); } expect(value).toBe(1); });
it("t19", () => { if (flag) { doThing(); } expect(value).toBe(1); });
