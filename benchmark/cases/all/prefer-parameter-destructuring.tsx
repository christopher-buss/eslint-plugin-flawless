// Coarse benchmark fixture for flawless/prefer-parameter-destructuring.
// Each function destructures its param in the body; the rule moves it to the signature.
function pd0(obj) { const { a } = obj; console.log(a); }
function pd1(obj) { const { a } = obj; console.log(a); }
function pd2(obj) { const { a } = obj; console.log(a); }
function pd3(obj) { const { a } = obj; console.log(a); }
function pd4(obj) { const { a } = obj; console.log(a); }
function pd5(obj) { const { a } = obj; console.log(a); }
function pd6(obj) { const { a } = obj; console.log(a); }
function pd7(obj) { const { a } = obj; console.log(a); }
function pd8(obj) { const { a } = obj; console.log(a); }
function pd9(obj) { const { a } = obj; console.log(a); }
function pd10(obj) { const { a } = obj; console.log(a); }
function pd11(obj) { const { a } = obj; console.log(a); }
function pd12(obj) { const { a } = obj; console.log(a); }
function pd13(obj) { const { a } = obj; console.log(a); }
function pd14(obj) { const { a } = obj; console.log(a); }
function pd15(obj) { const { a } = obj; console.log(a); }
function pd16(obj) { const { a } = obj; console.log(a); }
function pd17(obj) { const { a } = obj; console.log(a); }
function pd18(obj) { const { a } = obj; console.log(a); }
function pd19(obj) { const { a } = obj; console.log(a); }
