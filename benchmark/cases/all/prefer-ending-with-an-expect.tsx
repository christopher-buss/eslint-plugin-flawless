// Coarse benchmark fixture for flawless/prefer-ending-with-an-expect.
// Each test's last statement is not an assertion — the rule reports each one.
it("t0", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t1", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t2", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t3", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t4", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t5", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t6", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t7", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t8", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t9", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t10", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t11", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t12", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t13", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t14", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t15", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t16", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t17", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t18", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
it("t19", () => { expect(divide(10, 2)).toBe(5); cleanup(); });
